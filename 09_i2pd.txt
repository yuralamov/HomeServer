# i2pd роутер
sudo apt update && sudo add-apt-repository ppa:purplei2p/i2pd
sudo apt update && sudo apt upgrade -y && sudo apt install i2pd
sudo systemctl enable i2pd.service
# Правила для ufw
sudo ufw allow 7070
sudo ufw allow 4444
sudo ufw allow 4447
# На сервере
sudo nano /etc/i2pd/i2pd.conf
# Меняем 127.0.0.1 на интерфейс сервера 192.168.0.8
sudo systemctl restart i2pd.service
#####################
# На клиенте ставим
# https://github.com/PurpleI2P/i2pdbrowser
# Запуск для настройки firefox ~какаяплатформа~/build/build(.cmd)
# В сетевых настройках firefox укажем свой сервер 192.168.0.8
